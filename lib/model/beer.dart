import 'package:meta/meta.dart';

class Beer {
  final String id;
  final String name;
  final String description;

  Beer({
    @required this.id,
    @required this.name,
    @required this.description,
  });
}